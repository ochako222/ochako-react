import React from 'react';

import './index.scss';

const App: React.FC = () => {
    const foo = 'hi';
    console.log(foo);

    return (
        <div>
            <h1>Hello</h1>
        </div>
    );
};

export default App;
